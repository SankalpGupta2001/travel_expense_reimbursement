const currency=a=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:2}).format(Number(a)||0);
export default function TravelSummaryCard({request}){if(!request)return null;const d=request.travelDetails||{};return <div className="card"><div className="card-header"><h2>{request.travelRequestId}</h2><span>{request.employeeDetails?.employeeName} · {request.employeeDetails?.employeeCode}</span></div><div className="details-grid"><div className="detail-item"><span>Travel Dates</span><strong>{d.fromDate} → {d.toDate}</strong></div><div className="detail-item"><span>Duration</span><strong>{d.numberOfDays} days</strong></div><div className="detail-item"><span>Destination</span><strong>{d.destination}</strong></div><div className="detail-item"><span>Company / Customer</span><strong>{d.company}</strong></div><div className="detail-item"><span>Category</span><strong>{d.travelCategory}</strong></div><div className="detail-item"><span>Mode</span><strong>{d.modeOfTravel}</strong></div><div className="detail-item"><span>Estimated Cost</span><strong>{currency(request.estimatedCost?.total)}</strong></div><div className="detail-item"><span>Advance Requested</span><strong>{currency(request.travelAdvance?.requested)}</strong></div></div><div><span>Purpose</span><p>{d.purpose}</p></div></div>}
